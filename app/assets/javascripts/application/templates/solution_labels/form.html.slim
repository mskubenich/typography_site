.wrapper.wrapper-content
  .row.animated.fadeInRight
    .col-md-12
      .ibox.float-e-margins
        .ibox-title
          h3 ng-show="$state.current.name == 'new_solution_label'"
            | {{ I18n.t('solution_label.new' ) }}
          h3 ng-show="$state.current.name == 'edit_solution_label'"
            | {{ I18n.t('solution_label.edit' ) }}
        .ibox-content
          form ng-submit="submitSolutionLabel()" name="SolutionLabelForm" novalidate=true
            .form-group
              .row
                .col-sm-2
                  label.control-label
                    | {{ I18n.t('solution_label.fields.title') }}:
                .col-sm-10
                  input.form-control type="text" placeholder="{{ I18n.t('solution_label.fields.title') }}" required=true class="{{ SolutionLabelForm.title.$invalid && submitted ? 'validation-error' : ''}}" name="title" ng-model="solution_label.title"
                  .error ng-show="SolutionLabelForm.title.$invalid && submitted"
                    | {{ I18n.t('validations.cant_be_blank')}}

                  .error ng-repeat="error in validation_errors['title']"
                    | {{ error }}

              .hr-line-dashed

            .form-group
              .row
                .col-sm-4.col-sm-offset-2
                  button.ladda-button.btn.btn-info data-style="expand-left" ladda="formPending"
                    | {{ I18n.t('actions.submit') }}
